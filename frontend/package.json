{
  "name": "kwid-header-frontend",
  "version": "1.0.0",
  "description": "Optional Svelte micro-frontend for the KWID header/hero panel. Builds a static bundle embedded into the Streamlit app via streamlit.components.v1.html. The Python app runs perfectly well without this build (it degrades to native Streamlit markup) — build this only if you want the richer animated header.",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist --emptyOutDir"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.0",
    "svelte": "^4.2.0",
    "vite": "^5.2.0"
  }
}
